# Create empty assessment raw data frame

Returns an empty data frame with expected column structure for
assessment data.

## Usage

``` r
create_empty_assessment_raw()
```

## Value

Empty data frame with assessment columns
