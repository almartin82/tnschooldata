# Pad district ID to 4 digits

Tennessee district IDs are 4 digits, zero-padded

## Usage

``` r
pad_district_id(x)
```

## Arguments

- x:

  Vector of district IDs

## Value

Character vector of zero-padded district IDs
